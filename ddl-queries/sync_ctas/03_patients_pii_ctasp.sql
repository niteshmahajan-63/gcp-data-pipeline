CREATE OR REPLACE TABLE `sync_ctas.patients_pii_ctasp`
(
    patientid STRING,
    name STRING,
    remoteid INTEGER,
    mobileid INTEGER,
    emailid INTEGER,
    alternatemobileid INTEGER,
    entitydoctorid INTEGER,
    entityreferalid INTEGER,
    age INTEGER,
    bookingid STRING,
    bookingids ARRAY<STRING>,
    visitgap INTEGER,
    visitcount INTEGER,
    centreid INTEGER,
    channelid INTEGER,
    patienttypeid INTEGER,
    nationalityid INTEGER,
    insertedon TIMESTAMP,
    updatedon TIMESTAMP,
    dob DATE,
    joiningdate DATE,
    lastvisitdate DATE,
    deceased BOOLEAN,
    blocked BOOLEAN,
    gender STRING,
    mobile STRING,
    email STRING,
    hasvalidmobile BOOLEAN,
    hasvalidemail BOOLEAN,
    channel STRING,
    lastcentrename STRING,
    servicetype STRING,
    patienttype STRING,
    type STRING,
    labname STRING,
    encounterid STRING
)
